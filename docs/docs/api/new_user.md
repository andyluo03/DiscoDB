# New User

## ``POST`` /new_user

Headers:

```json
token: <jwt-token> 
user-id: <user-id>
```

Body:

```json
{
    "new_user" = "<new_username>",
    "new_pwd" = "<new_password>"
}
```

Response:

```json
N/A
```
